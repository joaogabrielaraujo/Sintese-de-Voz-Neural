---
phase: 14-ui-redesign
plan: 07
type: execute
wave: 6
depends_on: ["14-06"]
files_modified:
  - lib/ui/widgets/reader_document_view.dart
  - test/ui/redesign_widgets_test.dart
autonomous: true
requirements: []
gap_closure: true
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Sentence tap recognition survives playback-driven rebuilds between pointer down and pointer up."
    - "Active and pending sentence states remain visually and semantically distinguishable."
    - "Reader/player and mandatory states are covered at compact, intermediate, and wide widths without pagination."
  artifacts:
    - path: "lib/ui/widgets/reader_document_view.dart"
      provides: "Stable sentence recognizers and accessible current/pending semantics"
    - path: "test/ui/redesign_widgets_test.dart"
      provides: "Gesture lifecycle and compact/intermediate/wide UI regression matrix"
  key_links:
    - from: "ReaderDocumentView.didUpdateWidget"
      to: "TapGestureRecognizer lifecycle"
      via: "reconcile only when sentence identity changes"
      pattern: "_reconcileRecognizers"
    - from: "test/ui/redesign_widgets_test.dart"
      to: "ReaderPage LayoutBuilder"
      via: "390, intermediate below 900, and 1200 pixel viewports"
      pattern: "physicalSize"
---

<objective>
Close the remaining gesture/accessibility and responsive verification gaps while preserving the continuous editorial reader.

Purpose: ensure Android touch gestures and Windows mouse/keyboard use the same stable sentence interaction, and prove the final reader/player at all specified width classes (D-01, D-02, D-03).
Output: lifecycle-safe sentence interactions and a complete responsive widget matrix.
</objective>

<execution_context>
@C:/Users/55759/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/55759/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-ui-redesign/14-CONTEXT.md
@.planning/phases/14-ui-redesign/14-UI-SPEC.md
@.planning/phases/14-ui-redesign/14-VERIFICATION.md
@.planning/phases/14-ui-redesign/14-REVIEW.md
@.planning/phases/14-ui-redesign/14-PATTERNS.md
@.planning/phases/14-ui-redesign/14-06-SUMMARY.md
@lib/ui/widgets/reader_document_view.dart
@lib/ui/widgets/reader_page.dart
@lib/ui/widgets/audio_player_control_bar.dart
@lib/ui/app_theme.dart
@test/ui/redesign_widgets_test.dart

<interfaces>
- ReaderDocumentView receives immutable sentence list, activeIndex, pendingIndex, and onSentenceTap.
- ReaderPage uses wide at 900 px; compact/intermediate share the vertical reader composition.
- AudioPlayerControlBar uses compact controls below 430 px.
- Plan 14-06 adds live metric and action-availability parameters to ReaderPage fixtures.
</interfaces>

The original-page pagination analysis remains deferred. Tests must assert continuous chapter composition only.
</context>

## Artifacts this phase produces

- `lib/ui/widgets/reader_document_view.dart`: `_recognizersBySentenceIndex`, `_reconcileRecognizers`, and semantic labels for current/selected sentences.
- `test/ui/redesign_widgets_test.dart`: pointer-down/rebuild/pointer-up regression; 390 px compact, 700–899 px intermediate, and 1200 px wide reader/player cases; import state matrix assertions against final widget contracts.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve one sentence gesture through a playback rebuild</name>
  <files>lib/ui/widgets/reader_document_view.dart, test/ui/redesign_widgets_test.dart</files>
  <read_first>
    - lib/ui/widgets/reader_document_view.dart recognizer creation/disposal
    - lib/core/text/sentence_model.dart identity fields
    - test/ui/redesign_widgets_test.dart ReaderPage fixtures
    - .planning/phases/14-ui-redesign/14-REVIEW.md WR-01 and WR-04
    - .planning/phases/14-ui-redesign/14-UI-SPEC.md active/pending sentence states
  </read_first>
  <behavior>
    - Pointer down on a sentence, rebuild with a changed active index, then pointer up still invokes that sentence callback once.
    - Recognizers are reused while sentence identities are unchanged and disposed only when removed or when the widget disposes.
    - Active sentence semantics identify it as current; pending sentence semantics identify selection/confirmation state.
  </behavior>
  <action>
    Move recognizer creation/disposal out of `build`. Maintain recognizers keyed by stable sentence index, initialize/reconcile them in lifecycle methods, update callbacks without replacing retained recognizers, and dispose only removed entries or the entire map at widget disposal. Preserve continuous `RichText` rendering and the existing paper/amber/teal styles; do not turn sentences into cards or add page containers. Add per-span semantic labels or an equivalent semantics structure that communicates current and pending state while keeping the active live announcement restrained. Write a pointer lifecycle test that rebuilds between down/up and semantics tests for active/pending labels.
  </action>
  <verify>
    <automated>C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart test --no-pub test/ui/redesign_widgets_test.dart --plain-name "sentence gesture survives playback rebuild"</automated>
  </verify>
  <acceptance_criteria>
    - Playback-position rebuild does not cancel an in-progress sentence tap.
    - No recognizer is disposed from `build`.
    - Active and pending states preserve UI-SPEC colors/decorations and expose semantic state.
  </acceptance_criteria>
  <done>Touch and mouse sentence selection remains reliable while playback updates the active highlight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete compact, intermediate, wide, and mandatory-state coverage</name>
  <files>test/ui/redesign_widgets_test.dart</files>
  <read_first>
    - test/ui/redesign_widgets_test.dart after Task 1
    - lib/ui/widgets/reader_page.dart after Plan 14-06
    - lib/ui/widgets/audio_player_control_bar.dart after Plan 14-06
    - lib/ui/widgets/library_view.dart after Plan 14-04
    - .planning/phases/14-ui-redesign/14-UI-SPEC.md responsiveness and mandatory states
    - .planning/phases/14-ui-redesign/14-VERIFICATION.md automated coverage gap
  </read_first>
  <behavior>
    - At 390 px, compact reader, compact player controls, continuous text, selection confirmation, and live metrics render without exceptions.
    - At an intermediate width from 700 through 899 px, vertical reader/player composition remains usable and does not switch to NavigationRail/wide reader.
    - At 1200 px, wide reader/player and metrics remain visible without overflow.
    - Library tests cover import processing, cancelled, invalid/error, and idle/success states from Plan 14-04.
  </behavior>
  <action>
    Expand the final widget suite using explicit physical sizes and reset teardown. Build ReaderPage fixtures with the final telemetry and nullable-action contracts from Plan 14-06. Assert stable layout/control keys, no Flutter overflow exception, visible RTF/queue/memory labels, continuous ReaderDocumentView, and selection confirmation at 390 px, one 700–899 px intermediate size, and 1200 px. Extend LibraryView state assertions for dedicated import busy, cancelled, invalid/error, and idle/success presentation; ensure busy tap does not invoke import. Keep the locked 900 px shell breakpoint and continuous chapter reader. Do not introduce or assert EPUB page numbers, page turns, or original-page boundaries.
  </action>
  <verify>
    <automated>C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart test --no-pub test/ui/redesign_widgets_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Compact, intermediate, and wide reader/player tests all pass with no overflow exception.
    - Import processing, cancel, error, and completed/idle states are asserted.
    - Existing bottom navigation below 900 px and NavigationRail at/above 900 px remain unchanged.
    - No pagination model or test is added.
  </acceptance_criteria>
  <done>The automated widget suite covers the final Android/Windows reader, player, metrics, selection, and import-state contract across all width classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Pointer/keyboard events → sentence selection | User input crosses rebuilds and focus/semantics layers. |
| Runtime metrics/state → responsive layout | Dynamic labels can overflow compact or intermediate surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-07-01 | Denial of Service | gesture recognizer rebuild lifecycle | medium | mitigate | Stable keyed recognizers and pointer-down/rebuild/up regression. |
| T-14-07-02 | Tampering | active/pending visual state | low | mitigate | Single state source, preserved token mapping, semantics assertions. |
| T-14-07-03 | Denial of Service | compact/intermediate layout overflow | medium | mitigate | Explicit 390/intermediate/1200 viewport tests and wrapping controls/metrics. |
| T-14-07-04 | Information Disclosure | semantics labels | low | accept | Labels expose only the same local sentence text/state already rendered on screen. |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Responsive editorial Android/Windows UI preserving EPUB/TTS/audio/metrics flows | 14-02–14-07 | COVERED | Persistence, synchronization, telemetry and final UI tests close every failed/partial truth. |
| REQ | — | No Phase 14 requirement IDs are mapped | — | N/A | `requirements: []` is intentional; no IDs were invented. |
| RESEARCH | — | Research skipped by gap-closure contract | — | N/A | `14-PATTERNS.md` supplies local analogs and all applicable constraints are assigned. |
| CONTEXT | D-01 | Mockup is visual reference, not embedded HTML | 14-07 | COVERED | Existing Flutter widgets/tokens preserved. |
| CONTEXT | D-02 | Android vertical touch-first reader/navigation | 14-04, 14-07 | COVERED | Import/delete interactions and compact tests. |
| CONTEXT | D-03 | Windows wide mouse/keyboard UI | 14-03, 14-07 | COVERED | Serialized interactions and wide tests. |
| CONTEXT | D-04 | Preserve offline pipeline/import/queue/memory/RTF/MOS boundaries | 14-02–14-06 | COVERED | Defects fixed through existing services and bounded queue. |
| CONTEXT | D-05 | No runtime network fonts | 14-07 | COVERED | No font/dependency network behavior added. |
| CONTEXT | Deferred | EPUB original-page pagination | NONE | EXCLUDED | Explicitly deferred; continuous chapter reading retained. |

<verification>
- Run redesign widget tests, then all Phase 14 gap suites from Plans 14-02 through 14-06.
- Run direct Flutter analysis over every modified production/test file from the gap plans.
- Confirm no same-wave plan overlaps files and no pagination artifact exists in the new plan outputs.
</verification>

<success_criteria>
- Stable gesture lifecycle and semantic active/pending state pass.
- Compact, intermediate, and wide reader/player tests pass.
- Import processing/error/cancel states are covered.
- Every failed/partial verification truth is covered by executable tests in Plans 14-02–14-07.
</success_criteria>

<output>
Create `.planning/phases/14-ui-redesign/14-07-SUMMARY.md` when done.
</output>
