---
phase: 14-ui-redesign
plan: 06
type: execute
wave: 5
depends_on: ["14-05"]
files_modified:
  - lib/main.dart
  - lib/ui/widgets/reader_page.dart
  - lib/ui/widgets/audio_player_control_bar.dart
  - test/ui/telemetry_flow_test.dart
  - test/ui/audio_player_widget_test.dart
autonomous: true
requirements: []
gap_closure: true
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "RTF, MOS, academic report, queue depth/capacity, and memory allocation/freeing are reachable from real streamed production data."
    - "MOS and report controls are disabled with an explicit explanation until valid telemetry exists, then become functional."
    - "Final queue and memory statistics remain visible after cancellation/completion without retaining PCM."
  artifacts:
    - path: "lib/main.dart"
      provides: "Live/final queue-memory snapshots and functional MOS/report wiring"
    - path: "lib/ui/widgets/reader_page.dart"
      provides: "Responsive RTF/queue/memory/report surface"
    - path: "lib/ui/widgets/audio_player_control_bar.dart"
      provides: "Availability-aware MOS control"
    - path: "test/ui/telemetry_flow_test.dart"
      provides: "Production telemetry and action reachability regression"
  key_links:
    - from: "CircularAudioBuffer"
      to: "StreamingTelemetrySnapshot"
      via: "queue length/capacity and MemoryManager.stats snapshots"
      pattern: "memoryManager\\.stats"
    - from: "StreamingTelemetrySnapshot"
      to: "MOSEvaluationDialog and academic report"
      via: "nullable availability callbacks"
      pattern: "onOpen(Mos|Report)"
---

<objective>
Expand the proven live RTF path into complete production telemetry and honest MOS/report controls.

Purpose: satisfy the UI contract that TCC metrics remain reachable without polluting reading or retaining PCM, at compact Android and wide Windows sizes.
Output: queue/memory presentation, enabled/disabled action states, functional MOS/report, and app-level regressions.
</objective>

<execution_context>
@C:/Users/55759/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/55759/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-ui-redesign/14-CONTEXT.md
@.planning/phases/14-ui-redesign/14-UI-SPEC.md
@.planning/phases/14-ui-redesign/14-VERIFICATION.md
@.planning/phases/14-ui-redesign/14-REVIEW.md
@.planning/phases/14-ui-redesign/14-PATTERNS.md
@.planning/phases/14-ui-redesign/14-05-SUMMARY.md
@lib/core/pipeline/streaming_telemetry.dart
@lib/core/memory/circular_audio_buffer.dart
@lib/core/memory/memory_stats.dart
@lib/main.dart
@lib/ui/widgets/reader_page.dart
@lib/ui/widgets/audio_player_control_bar.dart

<interfaces>
- StreamingTelemetrySnapshot from Plan 14-05 supplies aggregate RTF/report data without PCM.
- CircularAudioBuffer exposes length, maxItems, and memoryManager.stats.
- ReaderPage composes one `_PlayerPane` in both compact and wide layouts.
- AudioPlayerControlBar currently requires a non-null MOS callback; change availability to nullable callbacks plus explanatory text/tooltip.
</interfaces>

All metrics remain local. Keep the continuous reader and the 700/900/430 responsive thresholds; original-page pagination stays out of scope.
</context>

## Artifacts this phase produces

- `StreamingTelemetrySnapshot` additions: `queueDepth`, `queueCapacity`, `MemoryStats memory`, `hasMetrics`, and copy/snapshot support for final queue state.
- `lib/main.dart`: `_publishStreamingTelemetry`, `_captureFinalTelemetry`, and availability-aware `_showMOSDialog` / `_showAcademicReportDialog` wiring.
- `ReaderPage`: nullable `onOpenMos`, nullable `onOpenReport`, `metricsUnavailableReason`, and wrapping `_MetricStrip`.
- `AudioPlayerControlBar`: nullable `onOpenMOSDialog` and disabled-state explanation semantics/tooltip.
- `test/ui/telemetry_flow_test.dart`: queue/memory labels, MOS dialog, report dialog, and final snapshot reachability.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Expose live and final queue/memory metrics with report access</name>
  <files>lib/main.dart, lib/ui/widgets/reader_page.dart, test/ui/telemetry_flow_test.dart</files>
  <read_first>
    - lib/core/pipeline/streaming_telemetry.dart after Plan 14-05
    - lib/core/memory/circular_audio_buffer.dart getters and cleanup order
    - lib/core/memory/memory_manager.dart and memory_stats.dart
    - lib/main.dart accepted item, release, completion, cancellation, reader close, app bar/report
    - lib/ui/widgets/reader_page.dart `_PlayerPane`
    - .planning/phases/14-ui-redesign/14-UI-SPEC.md metrics states
  </read_first>
  <behavior>
    - Accepted streaming data renders RTF, queue depth/capacity, allocated MB, and freed MB.
    - Queue release/cancel/completion publishes a final zero-depth memory snapshot before disposal erases access to the queue object.
    - Academic report opens from real snapshot data in the reader and remains reachable after returning to the library.
    - Compact and wide metric strips wrap without overflow.
  </behavior>
  <action>
    Extend the metadata-only snapshot with queue depth/capacity and immutable `MemoryStats`. In `main.dart`, publish a snapshot after accepted enqueue/dequeue, active-item release, completion, and cancellation; capture the final stats before nulling/disposal and keep them with the final run result. Do not retain the queue or any item in telemetry. Replace `_lastResult`-dependent report logic with `StreamingTelemetrySnapshot.generateAcademicReport`, keeping a reader report action and the home app-bar action connected to the same snapshot. In `ReaderPage`, add a wrapping `_MetricStrip` that presents RTF, queue, allocated and freed memory in the existing metric typography for compact/intermediate/wide constraints. Test real controlled stream data, final zero-depth snapshot, report dialog reachability, and no overflow.
  </action>
  <verify>
    <automated>C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart test --no-pub test/ui/telemetry_flow_test.dart --plain-name "production metrics and report stay reachable"</automated>
  </verify>
  <acceptance_criteria>
    - RTF, queue depth/capacity, allocated MB, and freed MB are derived from the active queue and visible.
    - Final snapshot is captured before queue disposal and contains no queue/item/audio reference.
    - The academic report opens from the live/final snapshot both in-reader and after returning to the library.
  </acceptance_criteria>
  <done>A reader can inspect real RTF, queue and memory behavior and open the academic report throughout and after a streamed run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make MOS and report availability honest and functional</name>
  <files>lib/main.dart, lib/ui/widgets/reader_page.dart, lib/ui/widgets/audio_player_control_bar.dart, test/ui/telemetry_flow_test.dart, test/ui/audio_player_widget_test.dart</files>
  <read_first>
    - lib/main.dart MOS/report methods and ReaderPage construction after Task 1
    - lib/ui/widgets/reader_page.dart after Task 1
    - lib/ui/widgets/audio_player_control_bar.dart MOS button
    - test/ui/audio_player_widget_test.dart existing enabled callback case
    - .planning/phases/14-ui-redesign/14-REVIEW.md CR-06
  </read_first>
  <behavior>
    - Before valid metrics, MOS and report controls are disabled and expose why they are unavailable.
    - After one accepted item, both controls enable; MOS opens, saves a rating, and report opens.
    - Disabled controls do not invoke callbacks; enabled controls do.
  </behavior>
  <action>
    Make MOS/report callbacks nullable from `main.dart` through `ReaderPage` and `AudioPlayerControlBar`. Before `snapshot.hasMetrics`, render the controls disabled with tooltip and semantic hint equivalent to “available after synthesis produces metrics”; do not leave enabled callbacks that return early. Once accepted metrics exist, pass functional callbacks to `MOSEvaluationDialog` and the report dialog. Preserve the existing amber/teal/ink visual tokens and minimum 44 px target. Extend the isolated player test for disabled/enabled behavior and the app-flow test to synthesize one item, open MOS, save a rating, and open the report from production wiring.
  </action>
  <verify>
    <automated>C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart test --no-pub test/ui/audio_player_widget_test.dart test/ui/telemetry_flow_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - No-metrics MOS/report controls are disabled and have an explanatory tooltip/semantic label.
    - Real accepted metrics enable both controls without restarting or leaving the reader.
    - App-level tests open and complete MOS and open the academic report using production callbacks.
  </acceptance_criteria>
  <done>MOS and report actions accurately reflect data availability and become fully functional from real streamed metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Queue/memory manager → UI | Mutable runtime counters become a retained immutable user-visible snapshot. |
| Telemetry availability → actions | UI controls trigger MOS/report only when supporting data exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-06-01 | Tampering | queue/memory snapshot | medium | mitigate | Snapshot at defined ownership transitions and use immutable copied scalars. |
| T-14-06-02 | Denial of Service | compact metric/player layout | medium | mitigate | Wrapping metric/control layout with compact/intermediate/wide widget tests. |
| T-14-06-03 | Repudiation | disabled action behavior | low | mitigate | Explicit availability predicate, disabled callback, explanatory semantics, app-level test. |
| T-14-06-04 | Information Disclosure | memory telemetry | low | accept | Only aggregate local buffer byte counts are displayed; no text/audio payload leaves the device. |
</threat_model>

<verification>
- Run telemetry flow and audio player widget tests together.
- Run direct analysis over all five modified files.
- Verify final telemetry survives queue disposal and no telemetry object owns queue/item/audio references.
</verification>

<success_criteria>
- RTF, MOS, report, queue, and memory are all reachable from real production streaming.
- Unavailable actions are disabled with a reason; available actions work.
- Queue/memory remain visible at compact, intermediate, and wide widths without overflow.
- PCM lifetime remains controlled solely by CircularAudioBuffer/MemoryManager.
</success_criteria>

<output>
Create `.planning/phases/14-ui-redesign/14-06-SUMMARY.md` when done.
</output>
