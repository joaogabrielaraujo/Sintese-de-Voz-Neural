---
phase: 14-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: ["14"]
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - lib/core/document/saved_book.dart
  - lib/core/document/saved_book_repository.dart
  - test/core/document/saved_book_test.dart
autonomous: true
requirements: []
gap_closure: true
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "crypto is a direct dependency resolved offline at 3.0.7, and its hosted pub.dev lockfile checksum/source metadata remains unchanged."
    - "EPUBs com bytes distintos nunca são deduplicados apenas por uma identidade curta ou por um digest coincidente."
    - "Metadados e payloads persistidos sobrevivem a reinício, escrita interrompida e regras de substituição do Windows e Android."
    - "Registros legados book-xxxxxxxx continuam legíveis sem perda de payload ou progresso."
  artifacts:
    - path: "pubspec.yaml"
      provides: "Direct crypto 3.0.7 dependency declaration"
    - path: "pubspec.lock"
      provides: "Stable hosted pub.dev crypto 3.0.7 source and checksum metadata"
    - path: "lib/core/document/saved_book_repository.dart"
      provides: "Identidade SHA-256 verificada, transações recuperáveis e limpeza de artefatos"
    - path: "lib/core/document/saved_book.dart"
      provides: "Checkpoint de sentença concluída compatível com JSON legado"
    - path: "test/core/document/saved_book_test.dart"
      provides: "Regressões de colisão, recuperação, reinício e compatibilidade"
  key_links:
    - from: "pubspec.yaml"
      to: "pubspec.lock"
      via: "offline dependency resolution retaining crypto 3.0.7 hosted checksum/source metadata"
      pattern: "dependency: \"direct main\""
    - from: "lib/core/document/saved_book_repository.dart"
      to: "pubspec.yaml"
      via: "sha256 do pacote crypto já resolvido no lockfile"
      pattern: "sha256\\.convert"
    - from: "lib/core/document/saved_book_repository.dart"
      to: "saved_books/*.json"
      via: "temp sibling, flush, backup e rename recuperável"
      pattern: "(tmp|backup|rename)"
    - from: "lib/core/document/saved_book_repository.dart"
      to: "saved_books/*.epub"
      via: "comparação de comprimento e bytes antes da deduplicação"
      pattern: "contentHash"
---

<objective>
Fechar a raiz de identidade e durabilidade da biblioteca EPUB com digest resistente a colisões, verificação de payload e transações recuperáveis.

Purpose: impedir alias silencioso, livros órfãos e perda de progresso no armazenamento privado do Android e do Windows, preservando a decisão de persistência local (D-04).
Output: repositório crash-safe, modelo compatível com checkpoints concluídos e testes reais de filesystem.
</objective>

<execution_context>
@C:/Users/55759/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/55759/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-ui-redesign/14-CONTEXT.md
@.planning/phases/14-ui-redesign/14-VERIFICATION.md
@.planning/phases/14-ui-redesign/14-REVIEW.md
@.planning/phases/14-ui-redesign/14-PATTERNS.md
@.planning/phases/14-ui-redesign/14-SUMMARY.md
@lib/core/document/saved_book.dart
@lib/core/document/saved_book_repository.dart
@test/core/document/saved_book_test.dart

<interfaces>
Existing contracts to preserve:
- SupportDirectoryProvider = Future&lt;Directory&gt; Function()
- SavedBookRepository.list/load/saveNew/update/delete
- SavedBookRecord JSON fields id, contentHash, chapterIndex, sentenceIndex, progress, updatedAt
- Existing IDs match book-[a-f0-9]{8}; new IDs must accept a full lowercase digest and an optional numeric collision suffix.
</interfaces>

<context_decisions>
- D-01: design_mockup.html remains a visual reference, not embedded HTML.
- D-02: Android remains touch-first and vertically composed.
- D-03: Windows remains mouse/keyboard-aware and wide-layout capable.
- D-04: EPUB, pipeline, queue, memory, RTF and MOS behavior stays offline and uses the existing service boundaries.
- D-05: no runtime network fonts or network dependency is introduced.
</context_decisions>

EPUB original-page pagination is outside this gap closure. Keep continuous chapter reading.
</context>

## Artifacts this phase produces

- `pubspec.yaml` / `pubspec.lock`: `crypto` 3.0.7 promoted from the already locked transitive package to a direct dependency, resolved offline.
- `lib/core/document/saved_book.dart`: `SavedBookRecord.completedSentenceIndex` with backward-compatible JSON defaults and `copyWith` support.
- `lib/core/document/saved_book_repository.dart`: `ContentDigestProvider`, `_payloadsEqual`, `_nextAvailableId`, `_writeMetadataTransaction`, and `_recoverTransactions`.
- `test/core/document/saved_book_test.dart`: deterministic legacy-FNV collision, injected digest collision, identical-payload, and restart cases.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Persist one collision-resistant EPUB path end to end</name>
  <reversibility rating="costly">The full-digest identifier becomes a durable on-disk contract; compatibility readers keep old IDs usable without rewriting existing files.</reversibility>
  <files>pubspec.yaml, pubspec.lock, lib/core/document/saved_book.dart, lib/core/document/saved_book_repository.dart, test/core/document/saved_book_test.dart</files>
  <read_first>
    - lib/core/document/saved_book_repository.dart
    - lib/core/document/saved_book.dart
    - test/core/document/saved_book_test.dart
    - pubspec.yaml
    - pubspec.lock lines 132-139 for the existing crypto 3.0.7 lock entry
    - .planning/phases/14-ui-redesign/14-PATTERNS.md sections for SavedBookRepository and saved_book_test
  </read_first>
  <behavior>
    - Offline dependency resolution succeeds with `crypto` declared under direct dependencies and leaves the existing `crypto` 3.0.7 hosted pub.dev checksum/source lock entry byte-for-byte stable.
    - Two verifier-provided byte sequences that share the old 32-bit FNV value produce distinct records and reload their own bytes.
    - Identical bytes deduplicate to the same record only after digest, length, and byte equality all agree.
    - An injected digest collision allocates a deterministic safe suffix and preserves both payloads.
    - A freshly persisted record reloads after constructing a new repository over the same support directory.
  </behavior>
  <action>
    Promote the already lockfile-resolved `crypto` 3.0.7 package to a direct dependency and resolve only from the local cache; abort if dependency resolution proposes a version, source, or checksum change. Replace `_fingerprint` with an injectable `ContentDigestProvider` whose production default is the full lowercase SHA-256 hex digest. Use `book-&lt;digest&gt;` and deterministic numeric suffixes for the exceptional case where the provider returns the same digest for different bytes. Before returning an existing record, load its payload and require equal byte length plus byte-for-byte equality. Expand `_isSafeId` to accept the existing eight-hex form and the new full-digest form with an optional numeric suffix, while rejecting separators and traversal. Add `completedSentenceIndex` to `SavedBookRecord`; legacy JSON lacking it must deserialize conservatively without claiming the current sentence completed. Implement tests first and retain D-04 offline application-support storage.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference = 'Stop'; $before = (Get-FileHash 'pubspec.lock' -Algorithm SHA256).Hash; &amp; 'C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe' 'C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart' pub get --offline; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $after = (Get-FileHash 'pubspec.lock' -Algorithm SHA256).Hash; if ($before -ne $after) { throw 'Offline resolution changed pubspec.lock' }; $pubspec = Get-Content -Raw 'pubspec.yaml'; if ($pubspec -notmatch '(?m)^  crypto:\s*\^?3\.0\.7\s*$') { throw 'crypto is not a direct 3.0.7 dependency' }; $lock = Get-Content -Raw 'pubspec.lock'; $entry = '(?ms)^  crypto:\r?\n    dependency: \"direct main\"\r?\n    description:\r?\n      name: crypto\r?\n      sha256: c8ea0233063ba03258fbcf2ca4d6dadfefe14f02fab57702265467a19f27fadf\r?\n      url: \"https://pub.dev\"\r?\n    source: hosted\r?\n    version: \"3\.0\.7\"'; if ($lock -notmatch $entry) { throw 'crypto lock metadata drifted' }; &amp; 'C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe' 'C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart' test --no-pub test/core/document/saved_book_test.dart; exit $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get --offline` succeeds without modifying `pubspec.lock`; `crypto` is `direct main` at 3.0.7 from hosted pub.dev with checksum `c8ea0233063ba03258fbcf2ca4d6dadfefe14f02fab57702265467a19f27fadf`.
    - The two distinct collision fixtures `B906FB55A0217D2B` and `DC3A4CA786B73DE7` have different record IDs and round-trip their original bytes.
    - An injected equal digest does not alias unequal payloads.
    - New contentHash values contain the full SHA-256 digest; legacy IDs remain accepted.
    - No digest implementation is hand-written and no network fetch occurs.
  </acceptance_criteria>
  <done>A user can import, restart, and reopen two formerly colliding EPUB payloads as distinct books while exact duplicate bytes still deduplicate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recover transactional metadata and clean interrupted writes</name>
  <files>lib/core/document/saved_book_repository.dart, test/core/document/saved_book_test.dart</files>
  <read_first>
    - lib/core/document/saved_book_repository.dart after Task 1
    - test/core/document/saved_book_test.dart after Task 1
    - .planning/phases/14-ui-redesign/14-REVIEW.md CR-03 and CR-04
    - .planning/phases/14-ui-redesign/14-PATTERNS.md required transactional behavior
  </read_first>
  <behavior>
    - A valid backup plus missing or malformed canonical JSON recovers the last valid record on list/load.
    - A stale temp file never supersedes a valid canonical record and is removed during recovery.
    - Update writes flush a same-directory temp, validate its JSON and ID, preserve a backup, and publish with Windows-safe rename ordering.
    - Failed initial metadata publication removes the unpublished EPUB payload and transaction artifacts.
  </behavior>
  <action>
    Add a same-directory metadata transaction protocol used by both `saveNew` and `update`: write and flush a uniquely named sibling temp file, decode it back to a `SavedBookRecord`, verify that the record ID matches the target, move any valid canonical file to a backup, then rename the temp to canonical using ordering that works when Windows refuses overwrite-on-rename. Restore the backup if publication fails. Run `_recoverTransactions` before `list` and `load`; recover a valid backup when canonical data is absent or invalid, retain a valid canonical when both exist, and remove stale temp/backup artifacts only after a valid canonical and payload pair is established. Publish payload before metadata on initial save, but delete the newly created payload if metadata cannot commit. Keep corrupt-record isolation and validated path construction. Use real temporary directories to test recovery layouts and cleanup on Android/Windows-compatible filesystem semantics.
  </action>
  <verify>
    <automated>C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart test --no-pub test/core/document/saved_book_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Restart tests recover the last valid metadata from backup after malformed or missing canonical JSON.
    - Stale transaction files are removed only after a valid canonical metadata/payload pair exists.
    - A failed initial metadata transaction leaves no canonical record and no orphan payload.
    - Direct in-place canonical JSON overwrite is no longer the update path.
  </acceptance_criteria>
  <done>Metadata updates are recoverable across interruption on Windows and Android, and repository startup repairs or cleans transaction residue without hiding valid books.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| EPUB bytes → local identity | Untrusted imported bytes influence deduplication and filenames. |
| saved_books directory → repository model | Truncated, stale, or manually altered local files are parsed into trusted records. |
| pubspec.lock → build | A package checksum/source change could alter the digest implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-02-01 | Tampering | `saveNew` identity/dedup | high | mitigate | Full SHA-256 plus payload length and byte equality before deduplication; safe suffixed ID on digest collision. |
| T-14-02-02 | Tampering | metadata replacement/recovery | high | mitigate | Flushed temp, validated decode, backup restore, same-directory rename, and startup recovery. |
| T-14-02-03 | Denial of Service | corrupt local records | medium | mitigate | Per-record isolation, bounded validation, cleanup of stale artifacts, and no library-wide failure. |
| T-14-02-04 | Elevation of Privilege | caller-supplied IDs | high | mitigate | Strict anchored ID grammar before every path construction; no separators or traversal. |
| T-14-02-SC | Tampering | dependency resolution | high | mitigate | Use the existing pub.dev `crypto` 3.0.7 lock entry and checksum, resolve offline, and abort on any source/version/checksum drift; no new package is selected. |
</threat_model>

<verification>
- Run offline dependency resolution while hashing `pubspec.lock` before and after, then assert the direct dependency classification, version, hosted source URL, and checksum.
- Run the complete repository test file through the direct Flutter tool entrypoint.
- Run direct Flutter analysis over `saved_book.dart`, `saved_book_repository.dart`, and `saved_book_test.dart` with `--no-pub`.
- Confirm temporary-directory fixtures cover canonical, backup, temp, orphan payload, legacy ID, digest collision, and restart paths.
</verification>

<success_criteria>
- Distinct payloads cannot alias even under the known old collision or an injected digest collision.
- Metadata publication and recovery are transactional under Windows and Android filesystem constraints.
- Existing eight-character records remain readable and no automatic destructive migration runs.
- Completed-sentence persistence data can be consumed by Plan 14-04.
</success_criteria>

<output>
Create `.planning/phases/14-ui-redesign/14-02-SUMMARY.md` when done.
</output>
