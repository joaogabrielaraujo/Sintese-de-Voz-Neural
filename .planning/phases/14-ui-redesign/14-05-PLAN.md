---
phase: 14-ui-redesign
plan: 05
type: execute
wave: 4
depends_on: ["14-03", "14-04"]
files_modified:
  - lib/core/pipeline/streaming_telemetry.dart
  - lib/main.dart
  - lib/ui/widgets/reader_page.dart
  - test/core/pipeline/streaming_telemetry_test.dart
  - test/ui/telemetry_flow_test.dart
autonomous: true
requirements: []
gap_closure: true
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The first accepted streamed item creates real live RTF data that is visible in the production reader."
    - "Streaming telemetry retains scalar metrics and sentence metadata, never PCM solely for measurement."
    - "Stale-generation items cannot contribute to telemetry."
  artifacts:
    - path: "lib/core/pipeline/streaming_telemetry.dart"
      provides: "Metadata-only live aggregation and academic report source"
    - path: "lib/main.dart"
      provides: "Accepted-generation telemetry accumulation"
    - path: "lib/ui/widgets/reader_page.dart"
      provides: "Live RTF presentation from production state"
    - path: "test/ui/telemetry_flow_test.dart"
      provides: "App-level synthesis-to-RTF reachability"
  key_links:
    - from: "SentenceAudioItem.metrics"
      to: "StreamingTelemetryAccumulator"
      via: "accepted generation item ingestion"
      pattern: "addAcceptedItem"
    - from: "lib/main.dart"
      to: "lib/ui/widgets/reader_page.dart"
      via: "StreamingTelemetrySnapshot passed to ReaderPage"
      pattern: "streamingTelemetry"
---

<objective>
Create the thinnest production telemetry path from one accepted streamed sentence to visible live RTF without retaining audio buffers.

Purpose: reconnect the redesigned reader to real pipeline measurements while preserving the bounded-memory queue and D-04's offline synthesis architecture.
Output: metadata-only streaming telemetry model, controller accumulation, reader RTF, and app-level reachability test.
</objective>

<execution_context>
@C:/Users/55759/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/55759/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-ui-redesign/14-CONTEXT.md
@.planning/phases/14-ui-redesign/14-UI-SPEC.md
@.planning/phases/14-ui-redesign/14-VERIFICATION.md
@.planning/phases/14-ui-redesign/14-PATTERNS.md
@.planning/phases/14-ui-redesign/14-03-SUMMARY.md
@.planning/phases/14-ui-redesign/14-04-SUMMARY.md
@lib/core/pipeline/pipeline_result.dart
@lib/core/pipeline/pipeline_orchestrator.dart
@lib/core/memory/sentence_audio_item.dart
@lib/main.dart
@lib/ui/widgets/reader_page.dart

<interfaces>
- SentenceAudioItem exposes rawSentence, normalizedText, audio, and PerformanceMetrics.
- PipelineResult defines the canonical aggregate formula: overallRtf = (totalInferenceTimeMs / 1000) / totalAudioDurationSeconds.
- Plan 14-03 supplies accepted-generation ownership checks; telemetry ingestion must happen only after those checks.
- ReaderPage currently accepts nullable rtf; replace the isolated scalar with a snapshot contract that Plan 14-06 can expand.
</interfaces>

No EPUB page model, page navigation, or original-page pagination is part of this plan.
</context>

## Artifacts this phase produces

- `lib/core/pipeline/streaming_telemetry.dart`: immutable `StreamingSentenceMetrics`, `StreamingTelemetrySnapshot`, and mutable controller-owned `StreamingTelemetryAccumulator`.
- `StreamingTelemetryAccumulator.addAcceptedItem(SentenceAudioItem)`: copies only sentence index/text counts and metric scalars.
- `StreamingTelemetrySnapshot.generateAcademicReport()`: report equivalent based on streamed metadata, without `AudioBuffer` or PCM lists.
- `test/core/pipeline/streaming_telemetry_test.dart`: formula, immutability, stale-exclusion seam, and no-PCM-lifetime tests.
- `test/ui/telemetry_flow_test.dart`: controlled app synthesis proving real RTF appears after one accepted item.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Stream one accepted sentence into visible live RTF</name>
  <files>lib/core/pipeline/streaming_telemetry.dart, lib/main.dart, lib/ui/widgets/reader_page.dart, test/core/pipeline/streaming_telemetry_test.dart, test/ui/telemetry_flow_test.dart</files>
  <read_first>
    - lib/core/pipeline/pipeline_result.dart aggregate fields and report formula
    - lib/core/memory/sentence_audio_item.dart
    - lib/core/memory/memory_stats.dart
    - lib/main.dart stream generation flow after Plans 14-03/14-04
    - lib/ui/widgets/reader_page.dart player status row
    - .planning/phases/14-ui-redesign/14-PATTERNS.md live metrics section
  </read_first>
  <behavior>
    - One accepted item with 100 ms inference and 2 s audio yields overall RTF 0.05 and one sentence in the snapshot.
    - Multiple accepted items aggregate inference/audio totals using the existing PipelineResult formula.
    - Clearing or purging the source item's PCM after ingestion does not change the snapshot or report.
    - A stale stream item released after replacement never changes the current snapshot.
    - In the pumped production page, accepted synthesis makes an `RTF` value visible.
  </behavior>
  <action>
    Create a metadata-only streaming telemetry model. `StreamingSentenceMetrics` may retain sentence index, word count, normalized text length, inference milliseconds, audio duration, and per-item RTF, but must not retain `AudioBuffer`, `Float32List`, WAV bytes, or `SentenceAudioItem`. `StreamingTelemetryAccumulator` owns scalar totals and produces immutable `StreamingTelemetrySnapshot` values with book/chapter identity, sentence/word counts, total inference, total audio duration, overall RTF, and academic report text equivalent to the existing `PipelineResult` report. In `main.dart`, create/reset the accumulator at a replacement stream start, ingest only after Plan 14-03 ownership checks accept the iterator item, and retain the latest snapshot across playback completion/reader close until a new import, chapter, or engine run explicitly replaces it. Pass the snapshot into `ReaderPage` and render real RTF in the existing status surface. Use the injected controlled stream/player to prove app-level reachability and release the source item's PCM after ingestion to prove metrics remain valid without retaining it. Preserve D-02/D-03 responsive composition and D-04 bounded queue behavior.
  </action>
  <verify>
    <automated>C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart test --no-pub test/core/pipeline/streaming_telemetry_test.dart test/ui/telemetry_flow_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Production controller assigns a non-null live snapshot after the first accepted stream item.
    - Reader RTF is sourced from that snapshot and matches the aggregate formula.
    - The telemetry model has no PCM/audio-buffer field and remains correct after queue purge.
    - Stale-generation stream items are excluded by a deterministic replacement test.
  </acceptance_criteria>
  <done>Starting production synthesis makes a correct live RTF visible after the first accepted item without extending PCM lifetime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Stream metrics → report/UI | Engine-provided timings become user-visible TCC measurements. |
| Queue item → telemetry snapshot | An object containing PCM crosses into a long-lived state model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-05-01 | Tampering | metric aggregation | medium | mitigate | Accept only current-generation items, sum typed finite scalar metrics, use canonical RTF formula. |
| T-14-05-02 | Denial of Service | telemetry retention | high | mitigate | Copy scalar metadata only; retain no PCM, WAV bytes, or SentenceAudioItem. |
| T-14-05-03 | Repudiation | report provenance | medium | mitigate | Snapshot carries book/chapter and accepted sentence counts/totals used by the report. |
| T-14-05-04 | Information Disclosure | report contents | low | accept | Report remains local/offline and contains existing book/chapter metadata and aggregate performance only. |
</threat_model>

<verification>
- Run the core telemetry model and app-flow telemetry tests together.
- Run direct analysis over the new model, `main.dart`, `reader_page.dart`, and tests.
- Inspect the new model's fields/imports to confirm no audio/typed-data dependency is retained.
</verification>

<success_criteria>
- Live production RTF is constructed from streamed item metrics and visible after synthesis begins.
- Aggregation agrees with `PipelineResult` math.
- Telemetry survives queue purge while PCM remains bounded by the existing queue.
- The model is ready for MOS/report/queue/memory expansion in Plan 14-06.
</success_criteria>

<output>
Create `.planning/phases/14-ui-redesign/14-05-SUMMARY.md` when done.
</output>
