---
context: phase
phase: 14-ui-redesign
task: android-device-uat
total_tasks: 3
status: paused
last_updated: 2026-08-02T22:15:03.927Z
---

# Phase 14 - Handoff Android G85

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These constraints were discovered through actual failures. Acknowledge them before running Android commands.

- [ ] CONSTRAINT: JDK location - the working JDK is `D:\Program Files\Android\Android Studio\jbr`; Flutter is already configured to use it. Do not conclude that Java is missing after checking only `C:`.
- [ ] CONSTRAINT: Codex Gradle home - sandboxed commands may resolve `GRADLE_USER_HOME` incorrectly as `C:\.gradle`. Set it to `C:\Users\55759\.gradle` for builds in this environment.
- [ ] CONSTRAINT: native verification - a successful APK build does not prove the app opens on the G85. Install, launch and capture `adb logcat` if it crashes.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Search only standard `C:` JDK paths | This produced the incorrect conclusion that no JDK existed. | blocking | Check the configured Flutter JDK and `D:\Program Files\Android\Android Studio\jbr` first. |
| Run Gradle without a valid user cache | Gradle attempted to create `C:\.gradle` and failed before configuration. | blocking | Export `GRADLE_USER_HOME=C:\Users\55759\.gradle` in Codex build commands. |
| Treat compilation as device success | The release APK compiled, but the phone was disconnected and runtime behavior remains unproven. | blocking | Require real install, launch and essential UAT on the G85. |
| Leave build daemons running | Gradle and Kotlin daemons retained large heaps after completion. | advisory | Run `android\gradlew.bat --stop`, then terminate only confirmed Gradle/Kotlin Java children if needed. |

<current_state>

Phase 14 remains paused before plan 14-04, with an Android compatibility/UAT detour in progress. The Android Gradle corrections are uncommitted. Flutter now uses the JDK 21 bundled with Android Studio on `D:`. A signed release APK restricted to `arm64-v8a` was generated successfully, but the Motorola G85 was disconnected, so installation and runtime behavior have not been verified.

Branch: `feature/tts-neural-functional`.
Last commit before this handoff update: `ed7823b wip: phase 14 paused at 14-04`.
</current_state>

<completed_work>

- Located and validated `D:\Program Files\Android\Android Studio\jbr` (OpenJDK 21.0.10) with Gradle 9.1.0.
- Registered that JDK globally in Flutter with `flutter config --jdk-dir`.
- Preserved the pending AGP 9 workaround that applies Kotlin/JVM 17 to `file_picker`.
- Restricted the Android package to `arm64-v8a` for the Motorola G85.
- Created a local release keystore at `D:\VoxReleaseKeys\vox-g85-local-release.jks`, alias `vox-g85`. The password is intentionally not stored in Git or this handoff.
- Built `build/app/outputs/flutter-apk/app-release.apk` successfully in release mode.
- Verified APK Signature Scheme v2, one RSA 4096 signer and only ABI `arm64-v8a`.
- Recorded APK size `111889790` bytes and SHA-256 `EEFB29AD9CDEBC9E4973924245BFE1CCEB31D07E0B18E6743EEC1022E832760B`.
- Stopped the Gradle and Kotlin compiler daemons after the build; no Java process from the build remained.
</completed_work>

<remaining_work>

1. Connect the Motorola G85 and authorize USB debugging.
2. Run `flutter devices`; use `idmobile` only if that exact ID appears.
3. Start with `flutter run -d idmobile` (or the detected ID) and verify that the corrected app opens.
4. If installation reports a signature mismatch, stop and ask before uninstalling the prior debug-signed app because uninstalling removes its local data.
5. If the app crashes, capture focused `adb logcat` evidence before changing code; compare with the prior `libdartjni.so`/JNI crash documented in `README.md`.
6. Validate native EPUB selection, offline TTS, audio playback and restart persistence on the G85.
7. Return to the formal phase sequence: 14-04, then 14-05/14-06, then full 14-07 Android/Windows UAT.
</remaining_work>

<decisions_made>

- Use the Android Studio JDK on `D:` rather than install another JDK.
- Keep the G85 test/release package ARM64-only.
- Use a private local release key outside the repository; never commit its password.
- Treat the APK as build-validated, not device-validated, until the G85 passes install and launch.
- Stop Gradle/Kotlin daemons after builds because the user requested resource cleanup.
</decisions_made>

<blockers>

- Human action: the Motorola G85 is disconnected.
- Possible signature transition: the previously installed APK was debug-signed, while the new APK is release-signed. Do not uninstall without approval.
- Release rebuilds require the local signing credential, which is intentionally absent from tracked files.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` - machine-readable current state and exact next action.
2. `README.md` Android validation section - prior G85 crash and reproduction history.
3. `android/app/build.gradle.kts` - ARM64 packaging and protected release signing.
4. `android/build.gradle.kts` - AGP 9/file_picker Kotlin workaround.
5. `.planning/phases/14-ui-redesign/14-07-PLAN.md` - full native Android/Windows UAT contract.
6. `.planning/phases/14-ui-redesign/14-04-PLAN.md` - next formal phase plan after device triage.

## Critical Anti-Patterns (do NOT repeat these)

- Do not search only `C:` for Java; use the configured JDK on `D:`.
- Do not claim `flutter run` works based solely on `flutter build apk`.
- Do not replace release signing with debug signing.
- Do not commit the keystore or its password.
- Do not uninstall the existing app without warning that local application data will be lost.
- Do not leave Gradle/Kotlin daemons running after the work is complete.

## Infrastructure State

- Flutter: 3.44.8 stable; Dart 3.12.2.
- JDK: `D:\Program Files\Android\Android Studio\jbr` (OpenJDK 21.0.10), registered in Flutter.
- Gradle: 9.1.0, cache at `C:\Users\55759\.gradle`.
- Android SDK: `C:\Users\55759\AppData\Local\Android\sdk`.
- Release key: `D:\VoxReleaseKeys\vox-g85-local-release.jks`, alias `vox-g85`; password intentionally omitted.
- APK: `build/app/outputs/flutter-apk/app-release.apk`.
- APK SHA-256: `EEFB29AD9CDEBC9E4973924245BFE1CCEB31D07E0B18E6743EEC1022E832760B`.
- Device: disconnected at pause time.
- Background processes: none from Gradle/Kotlin/JDK build.
- Async jobs: none.
- Uncommitted files: `README.md`, Android Gradle files shown by `git status`, and generated `android/build/` reports.

<context>

The build toolchain is now functional. The next uncertainty is exclusively the physical Android runtime: whether the corrected plugin registration removes the G85 startup crash and whether the application flows work end to end. Preserve the current dirty worktree and gather device evidence before further Gradle changes.
</context>

<next_action>

Start with: connect the Motorola G85, authorize USB debugging, run `flutter devices`, then run `flutter run -d idmobile` only if that ID is listed. Verify app launch before testing EPUB/TTS, and capture `adb logcat` on any crash. After finishing, stop Gradle/Kotlin daemons.
</next_action>
