---
context: phase
phase: 14-ui-redesign
task: 14-04
total_tasks: 3
status: in_progress
last_updated: 2026-08-01T22:36:12.770Z
---

# Phase 14 — Handoff de Retomada

<current_state>
A fase 14 está pausada após as implementações de persistência (14-02), transição segura/preparada de fala (14-03) e a primeira fatia do RF-07 de EPUB. A árvore Git estava limpa no momento da pausa.

Commits relevantes: `5fc8bb0` (persistência), `cf5acc7` (próxima frase preparada), `013ffaa` (nav.xhtml e imagens EPUB).
</current_state>

<completed_work>

- 14-02: SHA-256, IDs sem colisão, recuperação transacional e testes de persistência. `saved_book_test.dart` passou 12/12.
- 14-03: geração/identidade para streams, cancelamento serializado e pré-carregamento de uma próxima frase. O usuário confirmou que as pausas ficaram muito boas. `stream_cancellation_test.dart` passou 2/2 e a análise estática ficou limpa.
- RF-07 (fatia inicial): `EpubImageBlock`/`EpubTextBlock`, preservação de bytes de imagem, resolução de caminhos relativos, título via `nav.xhtml`, renderização de imagem e exclusão de imagem/alt da TTS. O usuário confirmou que imagens funcionam perfeitamente. `epub_parser_test.dart` passou 4/4.
</completed_work>

<remaining_work>

1. Executar 14-04: progresso baseado em frase concluída, persistência em lifecycle/reinício, importação/pesquisa/exclusão e regressões de UI.
2. Executar 14-05 e 14-06: conectar métricas reais do streaming à UI — RTF, MOS, relatório, profundidade de fila e memória. Não coletar métricas formais de MVP antes disso.
3. Executar 14-07: UAT Android e Windows, incluindo 20 frases consecutivas, persistência após encerramento real e controles nativos.
4. RF-07 follow-up: implementar fallback `toc.ncx` e níveis hierárquicos visuais do sumário. A implementação atual cobre `nav.xhtml` e títulos por arquivo do spine, mas ainda não representa entradas fragmentadas/níveis completos.
</remaining_work>

<decisions_made>

- EPUB: o sumário declarado pelo livro é a fonte de verdade; manter itens auxiliares, não filtrar capa/prefácio/notas.
- Imagens: renderizar toda imagem interna do XHTML na posição declarada e resolver `src` relativo dentro do ZIP; TTS ignora imagens e `alt` completamente.
- Áudio: pré-carregar somente uma frase; não inserir delays artificiais. Qualquer pausa residual é troca de fonte nativa do `audioplayers` e deve ser medida no UAT.
</decisions_made>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — estado estruturado e próxima ação.
2. `.planning/phases/14-ui-redesign/14-04-PLAN.md` — próximo plano executável.
3. `.planning/phases/14-ui-redesign/14-03-SUMMARY.md` — contrato de streaming e pré-carregamento.
4. `.planning/phases/14-ui-redesign/14-07-PLAN.md` — UAT Android/Windows e métrica de transição de fala.
5. `.planning/REQUIREMENTS.md` — RF-07.
6. `lib/core/document/epub_model.dart`, `epub_parser.dart`, `epub_bytes_importer.dart` e `lib/ui/widgets/reader_document_view.dart` — estado atual do suporte EPUB.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Executor em worktree vinculado | Agentes GSD recusaram commits porque `.git` é arquivo e a branch é `feature/tts-neural-functional`. | advisory | Deixar o executor editar/testar sem commit e o orquestrador registra commits via `gsd-tools query commit`. |
| Métricas apenas de UI | MOS/RTF podem parecer disponíveis sem dados reais de streaming. | advisory | Concluir 14-05/14-06 e validar produção antes do UAT de métricas. |

## Infrastructure State

- Branch: `feature/tts-neural-functional`, publicada em `origin`.
- O remoto avisou que mudou para `https://github.com/joaogabrielaraujo/Sintese-de-Voz-Neural.git`; o push anterior funcionou, mas atualizar `origin` é recomendável.
- Nenhum processo, servidor ou job externo pendente.

<next_action>
Começar pelo plano `14-04-PLAN.md`. Ao final, não avançar para métricas de MVP antes de concluir 14-05 e 14-06. Para retomar automaticamente, usar `$gsd-resume-work`.
</next_action>
