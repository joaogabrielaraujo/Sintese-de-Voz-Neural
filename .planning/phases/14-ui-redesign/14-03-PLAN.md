---
phase: 14-ui-redesign
plan: 03
type: execute
wave: 2
depends_on: ["14"]
files_modified:
  - lib/main.dart
  - test/ui/stream_cancellation_test.dart
autonomous: true
requirements: []
gap_closure: true
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After stop, chapter change, close, engine switch, or replacement, an older stream can never load, play, or publish sentence state."
    - "Every awaited streaming boundary revalidates generation and iterator identity before touching audio or UI state."
    - "Chapter changes and replacement starts are serialized behind completed cancellation."
  artifacts:
    - path: "lib/main.dart"
      provides: "Generation-owned stream lifecycle and serialized transitions"
    - path: "test/ui/stream_cancellation_test.dart"
      provides: "Deterministic stale-audio and replacement race regressions"
  key_links:
    - from: "lib/main.dart::_advanceStreamingSentence"
      to: "IAudioPlayerService"
      via: "generation and iterator checks after moveNext/load/play awaits"
      pattern: "_streamGeneration"
    - from: "lib/main.dart::_runMvpPipeline"
      to: "lib/main.dart::_stopStreamingPipelineUnlocked"
      via: "serialized transition chain"
      pattern: "_streamTransitionFuture"
---

<objective>
Eliminate stale streamed audio and reader-state races with explicit stream ownership and serialized replacement.

Purpose: keep the selected chapter, active highlight, queue and native player coherent across asynchronous cancellation on Android and Windows, while preserving the existing offline pipeline per D-04.
Output: generation-safe controller flow and deterministic cancellation tests.
</objective>

<execution_context>
@C:/Users/55759/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/55759/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-ui-redesign/14-CONTEXT.md
@.planning/phases/14-ui-redesign/14-VERIFICATION.md
@.planning/phases/14-ui-redesign/14-REVIEW.md
@.planning/phases/14-ui-redesign/14-PATTERNS.md
@.planning/phases/14-ui-redesign/14-SUMMARY.md
@lib/main.dart
@lib/core/audio/audio_player_service_interface.dart
@lib/core/pipeline/pipeline_orchestrator.dart
@lib/core/memory/circular_audio_buffer.dart

<interfaces>
- PipelineOrchestrator.processChapterStream returns Stream&lt;SentenceAudioItem&gt; and may await engine initialization, synthesis, enqueue, and dequeue.
- IAudioPlayerService.loadAudioBuffer/play/stop are asynchronous and stateStream reports completed.
- CircularAudioBuffer.cancel unblocks producer/consumer and release purges PCM.
- PoCNeuralHomePage already injects EpubDocumentPicker and EpubBytesImporter; add optional orchestrator/player dependencies with unchanged production defaults.
</interfaces>

EPUB pagination remains excluded. Preserve continuous chapter rendering and D-01 through D-05 UI decisions.
</context>

## Artifacts this phase produces

- `lib/main.dart`: `_streamGeneration`, `_streamTransitionFuture`, `_isCurrentStream`, `_serializeStreamTransition`, `_stopStreamingPipelineUnlocked`, and `_handleAudioCompleted`.
- `PoCNeuralHomePage`: optional `PipelineOrchestrator` and `IAudioPlayerService` injection parameters for deterministic app-flow testing.
- `test/ui/stream_cancellation_test.dart`: `ControlledPipelineOrchestrator` and `GatedRecordingAudioPlayer` test fakes using `Completer` gates.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one replacement stream cannot be overwritten by its predecessor</name>
  <files>lib/main.dart, test/ui/stream_cancellation_test.dart</files>
  <read_first>
    - lib/main.dart fields, `_initAudioListeners`, `_runMvpPipeline`, `_advanceStreamingSentence`, `_stopStreamingPipeline`, `_buildReaderScaffold`
    - lib/core/audio/audio_player_service_interface.dart
    - lib/core/memory/circular_audio_buffer.dart cancel/release/dispose contracts
    - test/core/memory/circular_audio_buffer_test.dart controlled cancellation case
    - .planning/phases/14-ui-redesign/14-PATTERNS.md stream generation section
  </read_first>
  <behavior>
    - Stream A pauses after `moveNext`; stream B replaces it; releasing A cannot call load/play or change active sentence/status.
    - Stream A pauses inside audio loading; cancellation and B replacement complete; releasing A cannot call play.
    - Only B's sentence index and audio identity are observed after replacement.
  </behavior>
  <action>
    Add a monotonically increasing `_streamGeneration`; invalidate it before cancelling any iterator/queue. Capture generation, iterator, and queue at the start of each advance and implement `_isCurrentStream` to require mounted state, matching generation, `_isStreaming`, identical iterator, and identical queue. Re-check ownership immediately after every awaited `moveNext`, `loadAudioBuffer`, `play`, iterator cancellation, and stop boundary before mutating `_activeStreamingItem`, sentence index, status, processing flags, queue ownership, or audio. Replace the global `_isAdvancingStream` ownership role with generation-scoped advancement so a blocked old advance cannot suppress B's first advance. Add optional orchestrator/player constructor injection without changing production defaults. Write deterministic widget/app tests with `Completer` gates and recording audio IDs; do not use arbitrary delays as race proof. Preserve D-04's existing offline orchestrator and bounded queue.
  </action>
  <verify>
    <automated>C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart test --no-pub test/ui/stream_cancellation_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The test releases stale continuations after B is active and records no A load/play/state mutation afterward.
    - Ownership checks appear after each await that precedes shared state or audio mutation.
    - B starts even while A's prior advance is unwinding.
  </acceptance_criteria>
  <done>Replacing a stream while the previous stream is suspended at either iterator or audio await leaves only the replacement audible and active.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize stop, chapter change, close, engine switch, and restart</name>
  <files>lib/main.dart, test/ui/stream_cancellation_test.dart</files>
  <read_first>
    - lib/main.dart after Task 1
    - test/ui/stream_cancellation_test.dart after Task 1
    - .planning/phases/14-ui-redesign/14-REVIEW.md CR-02
    - .planning/phases/14-ui-redesign/14-VERIFICATION.md reader synchronization gap
  </read_first>
  <behavior>
    - Chapter state changes only after old iterator cancellation and player stop complete.
    - Rapid chapter/restart requests execute in request order without overlapping ownership installation.
    - Stop, close, and engine switch leave no queue, iterator, active item, or playable stale buffer.
  </behavior>
  <action>
    Introduce `_serializeStreamTransition` backed by one future chain and a non-reentrant `_stopStreamingPipelineUnlocked`. Route `_runMvpPipeline`, chapter change, reader close, explicit stop, and engine switch through the serialized boundary. Each operation must invalidate generation first, await iterator cancellation and player stop where applicable, dispose/release the captured queue exactly once, then install chapter or replacement state. Keep exception handling generation-aware so an old failure cannot overwrite B's status. Have `_handleAudioCompleted` capture the active generation/item and refuse to advance if either changed. Extend the tests with rapid chapter/replacement and stop cases, asserting ordered cancellation and one release/dispose per owned item/queue.
  </action>
  <verify>
    <automated>C:\Users\55759\flutter\bin\cache\dart-sdk\bin\dart.exe C:\Users\55759\flutter\packages\flutter_tools\bin\flutter_tools.dart test --no-pub test/ui/stream_cancellation_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Cancellation finishes before new chapter fields or a new iterator are installed.
    - Rapid replacement tests produce one active generation and one audible stream.
    - Stop/close/engine-switch tests end with no retained queue, iterator, item, or stale play call.
  </acceptance_criteria>
  <done>All stream replacement entry points share one ordered lifecycle, preventing stale audio and state across Android touch actions and Windows keyboard/mouse actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Async stream → controller state | A continuation resumes after ownership may have changed. |
| Controller → native audio backend | A stale buffer can become audible if loaded or played after cancellation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-03-01 | Tampering | post-await controller mutation | high | mitigate | Monotonic generation plus iterator/queue identity checks after every await. |
| T-14-03-02 | Denial of Service | overlapping transition/cancellation | high | mitigate | Single serialized transition chain, cancellation before replacement, exactly-once cleanup. |
| T-14-03-03 | Repudiation | ambiguous active stream in tests | medium | mitigate | Recording fakes attach stream/audio IDs and assert the only accepted generation. |
| T-14-03-04 | Information Disclosure | stale chapter audio | medium | mitigate | Prevent any stale buffer from reaching the native player after ownership invalidation. |
</threat_model>

<verification>
- Run `test/ui/stream_cancellation_test.dart` with deterministic iterator and player gates.
- Run direct analysis over `lib/main.dart` and the new test.
- Confirm no test depends on elapsed-time sleeps to establish ordering.
</verification>

<success_criteria>
- Generation and identity checks guard every awaited streaming boundary.
- Chapter change and replacement starts wait for complete cancellation.
- The stale-audio regression is reproducible before the fix and green after it.
- Existing production engine/player defaults and continuous reader remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/14-ui-redesign/14-03-SUMMARY.md` when done.
</output>
