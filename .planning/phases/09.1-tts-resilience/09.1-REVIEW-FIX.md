---
phase: 09.1-tts-resilience
fixed_at: 2026-07-31T18:41:32.5636609Z
review_path: C:/Users/55759/Documents/sintese_de_voz/.planning/phases/09.1-tts-resilience/09.1-REVIEW.md
iteration: 1
findings_in_scope: 20
fixed: 17
skipped: 3
blocked: 2
status: partial
---

# Phase 09.1: Code Review Fix Report

**Fixed at:** 2026-07-31T18:41:32.5636609Z  
**Source review:** `C:/Users/55759/Documents/sintese_de_voz/.planning/phases/09.1-tts-resilience/09.1-REVIEW.md`  
**Iteration:** 1

## Summary

- Findings in scope: 20
- Fixed: 17
- Skipped or blocked: 3
- Commits: 0 (see Commit limitations)
- Primary outcome: inference failures now fail explicitly; no production engine or fallback may replace missing inference with fabricated silence.

## Fixed Issues

### CR-02: Sherpa failures are hidden and replaced with non-empty silence

**Files modified:** `lib/core/engine/tts_engine_interface.dart`, `lib/core/engine/sherpa_onnx_engine.dart`, `lib/core/engine/composite_tts_engine.dart`  
**Commit:** Not committed — the files overlap extensive pre-existing user changes.  
**Applied fix:** Added typed initialization/synthesis failures, rethrew Sherpa errors, removed estimated zero PCM, and made the composite reject empty, malformed, non-finite, or effectively silent output.

### CR-03: VitsOnnxEngine does not perform ONNX inference

**Files modified:** `lib/core/engine/vits_onnx_engine.dart`, `lib/core/engine/composite_tts_engine.dart`  
**Commit:** Not committed — both files were pre-existing uncommitted/untracked work.  
**Applied fix:** Disabled the pseudo-engine until a real ONNX Runtime session exists and removed it from production failover.

### CR-04: FlutterTtsEngine violates the AudioBuffer contract

**Files modified:** `lib/core/engine/flutter_tts_engine.dart`, `lib/core/engine/composite_tts_engine.dart`  
**Commit:** Not committed — both files were pre-existing uncommitted/untracked work.  
**Applied fix:** Removed native direct-speech from buffer failover, propagated setup failures, and made buffer synthesis fail explicitly instead of returning silent duration placeholders.

### CR-05: CLI engine is unreachable and misconfigured

**Files modified:** `lib/core/engine/composite_tts_engine.dart`, `lib/core/engine/sherpa_onnx_cli_engine.dart`, `lib/core/config/tts_config.dart`  
**Commit:** Not committed — the finding spans dirty and untracked files.  
**Applied fix:** Wired a distinct CLI engine into explicit selection and auto-failover; used `--vits-model`, `--vits-tokens`, `--vits-data-dir`, and `--output-filename`; required validated model/runtime paths.

### CR-06: EPUB text can become Windows shell commands

**Files modified:** `lib/core/engine/sherpa_onnx_cli_engine.dart`, `test/core/engine/sherpa_onnx_cli_engine_test.dart`  
**Commit:** Not committed — both files were pre-existing untracked work.  
**Applied fix:** All CLI execution now uses `runInShell: false`; a controlled process-runner test verifies text remains a separate argument and exact options are passed.

### CR-07: Exported Android system TTS service is a silence stub

**Files modified:** `android/app/src/main/AndroidManifest.xml`  
**Commit:** Not committed — the manifest already contained unrelated user modifications.  
**Applied fix:** Removed registration/export of the non-functional system TTS service. The stub is no longer advertised to Android clients while real native inference remains unimplemented.

### CR-09: Release APKs use debug signing

**Files modified:** `android/app/build.gradle.kts`  
**Commit:** Not committed — the build script already contained unrelated user modifications.  
**Applied fix:** Removed debug release signing, loaded release credentials from protected Gradle properties/environment variables, and made requested release tasks fail when credentials are absent. The example application ID remains unchanged because no owner-approved production ID was supplied.

### CR-10: Engine selector is absent and UI claims Sherpa is active

**Files modified:** `lib/main.dart`, `lib/core/engine/composite_tts_engine.dart`  
**Commit:** Not committed — the finding spans a dirty tracked file and an untracked file.  
**Applied fix:** Added supported runtime selection, stopped playback during switching, exposed the actual active engine, and surfaced initialization failures instead of showing a constant label.

### CR-11: Streaming consumers can wait forever

**Files modified:** `lib/core/pipeline/pipeline_orchestrator.dart`, `test/core/pipeline/pipeline_streaming_test.dart`  
**Commit:** Not committed — both files already contained user work.  
**Applied fix:** The queue is now the producer/consumer delivery path when supplied, a consumer runs concurrently with backpressure, and completion occurs in `finally`. Added over-capacity and synthesis-error tests.  
**Status:** Fixed; requires human verification of cancellation/backpressure semantics.

### WR-02: Model detection misclassifies the Piper model

**Files modified:** `lib/core/engine/onnx_model_detector.dart`, `test/core/engine/onnx_model_detector_test.dart`  
**Commit:** Not committed — both files were pre-existing untracked work.  
**Applied fix:** Removed token-count classification and required trusted model metadata (`model_type`, `comment`, `has_espeak`). Added coverage for the bundled 152-line Faber token file.  
**Status:** Fixed; requires human verification when new model families are introduced.

### WR-03: Windows post-build copy command is malformed

**Files modified:** `windows/runner/CMakeLists.txt`  
**Commit:** Not committed — the file already contained user modifications.  
**Applied fix:** Removed the redundant malformed `copy_directory` build event so packaging relies on Flutter/plugin bundled-library installation.

### WR-04: Multi-channel WAV sizes and pauses are incorrect

**Files modified:** `lib/core/audio/wav_writer.dart`, `lib/core/pipeline/pipeline_result.dart`, `test/core/audio/wav_writer_test.dart`, `test/core/pipeline/pipeline_result_test.dart`  
**Commit:** Not committed — the finding spans dirty and untracked files.  
**Applied fix:** Defined samples as interleaved values, corrected WAV data sizing, inserted pause frames for every channel, rejected format mismatches/incomplete frames, and added mono/stereo tests.  
**Status:** Fixed; requires human verification with real multi-channel playback.

### WR-05: Non-WAV input is blindly reinterpreted as Float32

**Files modified:** `lib/core/audio/audio_player_service.dart`, `lib/core/audio/wav_writer.dart`  
**Commit:** Not committed — both files contain pre-existing user work.  
**Applied fix:** `loadWavBytes` now validates RIFF/WAVE input and rejects invalid data; typed raw audio remains the responsibility of `loadAudioBuffer`.

### WR-06: WAV decoding accepts malformed/unsupported files

**Files modified:** `lib/core/audio/wav_writer.dart`, `test/core/audio/wav_writer_test.dart`  
**Commit:** Not committed — the implementation was already dirty and the test is new.  
**Applied fix:** Implemented bounded RIFF chunk parsing with padding, signature/size/format validation, PCM16-only enforcement, declared data bounds, and malformed/24-bit/float/truncated/odd-chunk tests.  
**Status:** Fixed; requires human verification against a broader WAV corpus.

### WR-07: maxSentenceLength is not enforced

**Files modified:** `lib/core/text/sentence_segmenter.dart`, `test/core/text/sentence_segmenter_test.dart`  
**Commit:** Not committed — both files were pre-existing untracked work.  
**Applied fix:** Added preferred-delimiter, whitespace, and hard-boundary fallback splitting; every emitted segment is tested against the configured maximum.  
**Status:** Fixed; requires human verification for Unicode grapheme boundaries.

### WR-08: Punctuation pause implementation is dead code

**Files modified:** `lib/core/audio/punctuation_pause_helper.dart`, `lib/core/pipeline/pipeline_result.dart`, `test/core/pipeline/pipeline_result_test.dart`  
**Commit:** Not committed — these files are untracked/dirty user work.  
**Applied fix:** Integrated deterministic punctuation-aware pauses into combined Float32 audio using the actual frame rate/channel count; playback-rate changes scale the complete WAV consistently.  
**Status:** Fixed; requires human listening verification.

### WR-09: Sentence highlighting ignores inserted pauses

**Files modified:** `lib/core/pipeline/pipeline_result.dart`, `lib/main.dart`, `test/core/pipeline/pipeline_result_test.dart`  
**Commit:** Not committed — the finding spans dirty and new files.  
**Applied fix:** Built an exact start/speech-end/end timeline during audio combination and used it for highlighting and sentence seeking.  
**Status:** Fixed; requires human UI/playback verification.

## Skipped or Blocked Issues

### CR-01: Required espeak-ng-data is absent

**File:** `lib/core/engine/sherpa_onnx_engine.dart:61-70,91-99`  
**Reason:** Blocked by an unavailable external model runtime asset. No `espeak-ng-data` was invented, downloaded, or synthesized. Safe support was added through `TTSConfig.espeakDataPath`, directory validation, `dataDir`, and removal of the fabricated demo lexicon; initialization now fails truthfully until a complete official directory is supplied.  
**Original issue:** The Piper voice requires complete `espeak-ng-data`, but the repository does not contain it.

### CR-08: Web fallback is not in the runtime graph

**File:** `lib/main.dart:3-8,78-84`  
**Reason:** Skipped: a safe correction requires a separate direct-speech pipeline/UI contract and conditional application factory. The existing Web engine also fabricates silent `AudioBuffer` values, so wiring it into the current buffer pipeline would reproduce the primary failure. No new Web claim was added.  
**Original issue:** Native-only imports remain in the Web compilation graph and Web Speech is disconnected.

### WR-01: Real packaged-model integration coverage is absent

**File:** `test/core/engine/composite_tts_engine_test.dart:23-49`  
**Reason:** Blocked by the missing official `espeak-ng-data` and unavailable packaged native runtime in this checkout. Existing misleading tests were hardened: mock tests are labeled as unit tests, silence/non-finite output is rejected, active engine identity is asserted, RMS-equivalent non-zero ratios are checked, and the CLI uses a controlled runner. A genuine device/desktop packaged-model playback test still cannot be truthfully added.  
**Original issue:** Tests accepted non-empty zero buffers as real inference.

## Changed Files

- `android/app/build.gradle.kts`
- `android/app/src/main/AndroidManifest.xml`
- `lib/core/audio/audio_player_service.dart`
- `lib/core/audio/punctuation_pause_helper.dart`
- `lib/core/audio/wav_writer.dart`
- `lib/core/config/tts_config.dart`
- `lib/core/engine/composite_tts_engine.dart`
- `lib/core/engine/flutter_tts_engine.dart`
- `lib/core/engine/onnx_model_detector.dart`
- `lib/core/engine/sherpa_onnx_cli_engine.dart`
- `lib/core/engine/sherpa_onnx_engine.dart`
- `lib/core/engine/tts_engine_interface.dart`
- `lib/core/engine/vits_onnx_engine.dart`
- `lib/core/pipeline/pipeline_orchestrator.dart`
- `lib/core/pipeline/pipeline_result.dart`
- `lib/core/text/sentence_segmenter.dart`
- `lib/main.dart`
- `windows/runner/CMakeLists.txt`
- `test/core/audio/wav_writer_test.dart` (new)
- `test/core/engine/composite_tts_engine_test.dart`
- `test/core/engine/onnx_model_detector_test.dart`
- `test/core/engine/sherpa_onnx_cli_engine_test.dart`
- `test/core/pipeline/pipeline_orchestrator_test.dart`
- `test/core/pipeline/pipeline_result_test.dart` (new)
- `test/core/pipeline/pipeline_streaming_test.dart`
- `test/core/sherpa_onnx_engine_test.dart`
- `test/core/text/sentence_segmenter_test.dart`
- `test/core/tts_engine_test.dart`

## Verification

Verification ran in the isolated fixer worktree before the safe edits were copied back to the main checkout.

- `dart format`: completed for 24 Dart files after invoking the SDK executable directly. The wrapper command had previously timed out.
- `dart analyze`: completed with **0 errors** and 9 informational lints. The remaining Web deprecation corresponds to skipped CR-08; other infos are non-blocking style/deprecation notices.
- Focused Flutter tests: **26 passed, 0 failed** across engine failover, controlled CLI arguments, Sherpa failure semantics, model detection, WAV parsing, streaming backpressure/error completion, timeline/pause combination, sentence segmentation, and pipeline/mock behavior.
- Android Gradle validation: unavailable because this repository has no `android/gradlew.bat` wrapper.
- Full suite/end-to-end/device playback: not run, per fixer scope and because the required external native/model runtime data is absent.

## Commit Limitations

No fix commit was created. The checkout contained extensive pre-existing tracked modifications and many reviewed source files were untracked. Every finding crossed at least one overlapping dirty/untracked file, so committing would have included unrelated user work and violated atomic per-finding commit safety. The 28 safe fixer-touched files were preserved in the main checkout only after a bounded timestamp/hash check confirmed none had changed there since isolation. `REVIEW-FIX.md` is intentionally uncommitted for the orchestrator.

---

_Fixed: 2026-07-31T18:41:32.5636609Z_  
_Fixer: the agent (gsd-code-fixer)_  
_Iteration: 1_
